events {}

http {
  client_max_body_size 50m;

  server {
    listen 80;
    server_name _;

    location / {
      return 200 "nginx placeholder for later deployment tasks\n";
      add_header Content-Type text/plain;
    }
  }
}
